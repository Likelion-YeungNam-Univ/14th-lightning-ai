# 명세 확정사항 (구현 기준 오버레이)

팀 번개조 | 2026.08 | 대상: `assit_요구사항명세서_v3.md` (PM 개정본 — 국내/해외 구분 반영)

> **읽는 법** — 구현 기준은 **v3 명세 + 이 문서**다. 두 문서가 어긋나면 **이 문서가 우선**한다.
> PM이 명세를 계속 개정하므로, 원본은 손대지 않고 백엔드 확정·수정 사항을 여기에 겹쳐 관리한다.
> (이전 v2.1처럼 원본을 직접 고쳐 통합하는 방식은 PM 개정 때마다 재작업이 생겨 폐기했다.)

---

## 0. 검토 결론

**v3 명세는 기술적·정책적으로 전부 구현 가능하다.** 해외 축의 두 신규 소스는 오히려 국내보다 조건이 좋다 — SEC EDGAR·Federal Register 모두 **API 키 불필요·무료·퍼블릭 도메인**이다. 단 두 가지 조건을 확정한다:

1. **개발 순서는 국내 축 우선.** 국내 축을 end-to-end로 먼저 완성(P1~P4)하고 해외 축(P3')을 그 뒤에 붙인다. 해외 축이 지연돼도 국내만으로 시연이 성립하는 구조를 유지한다.
2. **해외 화이트리스트는 15개 내외로 시작**한다(명세 제안 20~30개의 하한 밑). 종목당 수집·생성·검수 비용이 있으므로 시연에 실제로 쓸 만큼만. 목록은 팀 확정 필요(테슬라·엔비디아·애플 등 인지도순 권장).

## 1. LLM 변경 — OpenAI로 확정 (팀 결정)

| 항목 | 확정 |
| :---- | :---- |
| 생성 (요약·라벨·연결 문장·용어 풀이·해외 자료 한국어 요약) | **OpenAI API.** 모델 ID는 환경 변수 `OPENAI_MODEL`로 주입(기본값 `gpt-5-mini`) — 코드 수정 없이 교체 가능. structured output(JSON 스키마 강제) 사용 |
| 임베딩 (RAG) | OpenAI `text-embedding-3-small` (기존 확정 유지) |
| 키 | `OPENAI_API_KEY` 하나로 통일. Anthropic 키·의존성 제거 |

v3 명세 F-8.4의 "LLM 키"는 OpenAI 키를 뜻하는 것으로 읽는다.

## 2. 블로커 해소 (v3의 B1~B4 → 전부 닫힘)

| v3 블로커 | 확정 (P0에서 이미 구현·검증된 것 포함) |
| :---- | :---- |
| B1 국내 종목 마스터 | **KRX — FinanceDataReader** (`StockListing('KRX')`+`('KRX-DESC')`). 실측 완료: 2,713종목 적재, 시총 직접 제공 → **F-3.1 완료 상태** |
| B2 국내 업종 분류 | **자체 12분류** (부처 매핑표와 동일 축). KRX 업종명→12분류 키워드 규칙을 `data/industry_rules.json`으로 관리 → **F-3.1.2 완료 상태** |
| B3 금통위 결정 요지 | **ECOS 미제공 전제.** 기준금리는 ECOS `722Y001`, 결정 요지는 최근 1~2건 수동 시드 적재(연 8회 자료), 여유 시 한국은행 보도자료 수집으로 승격 |
| B4 라벨 오판 검증 | 시연 종목 생성물 **전수 검수 후 잠금**(F-5.7 locked), 비시연 탭별 10% 표본 검수 |

착수 확인 2건(V1 부처명 문자열 대조 / V2 KRX 필드) 중 **V2는 완료**, V1은 정책브리핑 키 발급 후 1회 호출로 확인.

## 3. 해외 축 신규 확정·제안

| 대상 | 확정 |
| :---- | :---- |
| F-3.2 화이트리스트 규모 | **15개 내외로 시작** (0절). 티커·영문명·한글 별칭·CIK·SIC를 `data/overseas_whitelist.json`으로 관리 |
| F-4.6 SEC 폼 유형 | 제안 채택: **8-K · 10-Q · 10-K** (+ 여유 시 6-K — 비미국계 기업용) |
| F-4.6 User-Agent | SEC 필수 요구. 환경 변수 `SEC_USER_AGENT`에 `"assit-hackathon <팀 대표 이메일>"` 형식으로 주입. **이메일 팀 확정 필요** |
| F-4.6.1 폼 유형 해설표 | 국내 공시유형 해설과 함께 **RAG 지식베이스(아래 5절)의 소스로도 적재** — 해외 공시 요약의 근거로 검색·주입 |
| F-4.7 Federal Register 문서 유형 | 제안 채택: **RULE(규칙) · PRORULE(제안 규칙)** + PRESDOCU 제외. 기관 필터 필수(연 8만 건) |
| F-4.7.1 SIC→기관 매핑 | 명세대로 LLM 초안 → `/api/v1/agencies` 유효 목록 대조 검증 → `data/`로 관리. 화이트리스트 15개의 SIC만 커버하면 되므로 매핑 규모 작음 |
| F-5.3 해외 연결 문장 입력 | 해외도 '업종 성격' 서술이 필요 — `industry_agency.profile` 컬럼에 SIC 그룹별 성격 서술을 데이터로 관리 (국내 C7과 동일 방식) |
| 유튜브 해외 검색 쿼리 | 국내 `"{종목명} 주식"` / 해외 `"{한글 별칭} 주식"` (별칭 없으면 영문명) |

## 4. 기존 확정값 재적용 (v2.1 C-시리즈 → v3 번호 매핑)

v2.1에서 확정했던 것들은 전부 유효하며 v3 번호로 다시 건다:

| 확정 내용 | v3 번호 |
| :---- | :---- |
| 세션 30일 / 모의 로그인 환경변수 / `/admin/reset-demo`에 **관리자 토큰**(`ADMIN_TOKEN`) | F-1.1 / F-1.2 / F-1.5 |
| 초성 검색 미지원, 최소 입력 이름 2자·코드 1자 / 구분별 등록 상한 30 / 인기 종목 시총 상위 8 고정 / 기본 종목 주 1회 갱신 / 삭제 종목의 저장 카드 유지 | F-3.3 / F-3.5 / (인기 종목은 v3에서 빠졌으나 유지) / F-3.8 / F-3.6 |
| 공시 유형 필터 없이 최신순 20건 / 유튜브 쿼리·10건 / 배치 06:00 KST / 재시도 3회 | F-4.1 / F-4.5 / F-4.8 / F-4.9 |
| 요약 2단 **한 번의 호출**로 생성(structured output) / 숫자 토큰 후처리(금액·수량형만, 날짜 제외) / 라벨 검증 절차(2절 B4) | F-5.1.1 / F-5.1.3 / F-5.2 |
| 용어 풀이: 입력 50자, 캐시 `용어+탭`, 캐시 우선→스트리밍 / 레이트리밋 분당 20회 | F-5.4 / F-8.3 |
| 페이지네이션 없음(탭당 최신 20건) / **공통 스키마에 `card_id` 추가**(저장 API가 받는 식별자) | F-6.1 / F-6.1.1 |
| F-6.6 원문 링크 제안 **채택**: `origin_url` 항상 응답에 담되 노출은 시트만 | F-6.6 |
| 저장 바디: `card_id`+`tab`+`stock_code` / **유튜브 스냅샷은 제목·링크만**(YouTube 30일 보관 정책) / `saved_card.source_item_id`는 nullable(원자료 3개월 롤링 삭제 대비) | F-7.1 / F-7.3 |
| 지표는 자체 `event_log` 테이블, 재방문 = 24시간 후 재진입 / 배포: Vercel rewrite 프록시 + **uvicorn 워커 1개 고정** | F-8.2 / (구 F-7.7 유지) |
| 기본 종목은 **보통주만**(국내 코드 끝자리 '0') — 시총 상위에 우선주(삼성전자우)가 끼는 것 방지 | F-3.8 |
| 데모 초기화 대상 = **요청 세션**(쿠키 기준). 로그인 상태도 함께 초기화해 첫 진입 화면 재현 | F-1.5 |
| 구분별 마지막 본 종목은 **카드 조회 시점에 갱신**하되 **등록된 종목 조회만** 반영. 등록 해제된 종목은 복귀 지점에서 제외(null) | F-2.2 |
| 카드 아이템은 #5(AI 가공) 전까지 **탭별 규칙을 지키는 목 데이터** — `[목데이터]` 접두사로 명시, 스키마는 최종본 | F-6.1 |

## 5. RAG 지식베이스 — v3에 없지만 유지 (신규 번호 F-5.8)

심사 요구("RAG 사용 시 기술 설명 가능")와 환각 억제를 위해 유지한다. v3 반영으로 소스가 하나 늘었다:

- 소스: ① 한국은행 경제금융용어 700선 ② **국내 공시유형 해설** ③ **미국 폼 유형 해설(F-4.6.1과 동일 데이터)**
- 파이프라인: 청킹 → OpenAI 임베딩 → pgvector 코사인 top-3 (유사도 하한 미달 시 미주입)
- 사용처: 용어 풀이(F-5.4), 국내 공시 요약(F-5.1), 해외 공시 요약(F-5.1)

구현 확정 (2026-08-14, 실측):

| 항목 | 확정 |
| :---- | :---- |
| 700선 원본 | 한국은행 2020년판 PDF → `scripts/parse_bok700.py`(1회성, pypdf)로 파싱 → `data/bok_700.json` **685건**(692 중 98.9%, 목차 대조 방식) |
| 청킹 | 용어 1건 = 청크 1개(정의 200~900자 — 추가 분할 불필요). 임베딩 입력은 `용어명\n정의` |
| 검색 순서 | **① 표제어 정확 일치**(사전 조회 — 'PER'→'주가수익비율(PER)', 임베딩 생략) → ② 벡터 검색 |
| 유사도 하한 | 코사인 **0.45** — 실측에서 미수록 용어에 0.41짜리 무관 근거가 잡혀 0.35에서 상향. 미달 근거는 주입하지 않는다 |
| 벡터 인덱스 | **미사용(전수 스캔)** — 701행 규모에선 1ms 미만·재현율 100%. 수만 청크로 커지면 HNSW 한 줄 전환(RAG설명.md 참조) |
| 국내 공시 해설 주입 | DART `report_nm`을 분류표(12유형, 데이터 관리) **부분 일치·긴 이름 우선**으로 `doc_type` 분류 → 요약 입력에 해설 첨부. 분류 불가 유형은 해설 없이 진행 |
| 용어 풀이 응답 | RAG 근거 목록(`sources`: 출처·용어·유사도)을 응답에 그대로 노출 — 심사 "설명 가능성" 대응 |
| 캐시 | `term_cache(term+tab)`. 캐시 히트는 LLM·레이트리밋 미소모. **가드레일 실패(빈 값)는 캐시하지 않는다** |
| 레이트리밋 (F-8.3) | 인프로세스 슬라이딩 윈도(워커 1 전제) — 세션당 20회/분(제안 채택) + IP당 60회/분(공유망 고려) |
| 임베딩 한도 | OpenAI TPM 40k 실측 — 적재 스크립트는 배치 50 + 429 대기 재시도 |

## 6. 데이터 모델 보정 (v3 부록 1 대비)

| 항목 | 확정 |
| :---- | :---- |
| `market` 값 | 소문자 `domestic` / `overseas` (탭 코드와 표기 통일) |
| `stock_master` | v3 컬럼 + **`exchange`**(KOSPI/KOSDAQ 등 시장구분 — v3 F-3.1 필수 컬럼인데 부록 1에서 빠짐) |
| `industry_agency` | v3 구조 + **`profile`**(업종 성격, C7 유지 — F-5.3 입력) + `name`(표시명) |
| `rate_link_sentence` | v3 키(market+industry_key+indicator_version)에 **`tab` 추가** — 국내는 한국은행·Fed 두 탭에 문장이 각각 있어 tab 없이는 충돌 |
| 유지 테이블 | `knowledge_chunk`(RAG)·`term_cache`·`event_log` — v3 부록 1에 없지만 유지 |
| `source_item.content` | **추가** — 규제 본문·금리 결정 요지 텍스트. AI 요약(F-5.1) 입력 전용, 화면 직접 노출 금지 |
| `collect_status` | **추가** (tab+scope_key PK) — 수집 단위별 성공/실패 기록. F-6.4의 `no_data` / `fetch_failed` 구분 근거 + 유튜브 24h 캐시 TTL 판정 |

## 6-1. 수집 구현 확정 (F-4, 실측 근거 — 2026-08-14)

| 항목 | 확정 | 근거 |
| :---- | :---- | :---- |
| 정책브리핑 | 신규 엔드포인트 **`policyNewsService2/policyNewsList2`** (구 `policyNewsService`는 신규 키 사용 불가). 파라미터는 serviceKey·startDate·endDate(YYYYMMDD) 3개뿐, **날짜 범위 최대 3일** → 90일을 3일 청크 31회로 수집. 인증키는 URL 디코딩 후 사용(이중 인코딩 방지). `ApproveDate`는 `MM/DD/YYYY HH:MM:SS` | 실측 + 포털 스웨거(2026-08-14) |
| 부처 매핑 | 2025 개편 명칭으로 실측 검증(산업통상부·기후에너지환경부 등 일치). **재정경제부 → finance 추가**(90일 실측 151건 중 키워드 통과 15건) | F-4.2.1 — 매핑은 데이터로 관리 |
| ECOS 기준금리 | `722Y001/D` + **항목코드 `0101000` 필터 필수** | 실측: 필터 없으면 다른 시리즈(2.586 등) 혼입 |
| FRED 미국 금리 | `DFEDTARU`(목표금리 상단), value `"."`(휴장일)는 스킵 | 실측 |
| 금리 탭 카드 구조 | 결정문은 `data/rate_decisions.json` 수동 시드(B3), 수집기는 최신 카드에 `indicator_value`(예 `2.75%`)·`doc_type`(인상/인하/동결)만 갱신 | ECOS·FRED는 요지 텍스트 미제공 |
| 유튜브 검색 | `order=viewCount` + **`publishedAfter` 90일** 병행 | 실측: 조회수순 단독은 수년 전 영상 혼입 |
| 유튜브 쿼터 | search 100 + videos 1 유닛, 일 10,000의 **80% 도달 시 신규 검색 중단**, 호출 직후 카운터 기록, 종목당 24h 캐시 | F-4.5.1 |
| 온디맨드 수집 | `POST /me/stocks` 성공 응답 후 FastAPI `BackgroundTasks`로 신규 종목만 수집(공시+유튜브) — 요청 경로 외부 API 금지(불변식 1) 준수 | F-4.10 |
| 보관 정리 | 90일 초과 원자료 삭제(일 배치 말미). **금리 탭(bok/fed)은 제외** — 결정문은 수동 시드 소수라 지우면 탭이 빈다 | F-4.8 |
| 배치 체인 | 일 06:00: 마스터→공시→유튜브→규제→금리→정리(실패 단위 격리). 주 1회 월 05:30: corp_code 갱신 | F-3.6, F-4.9 |
| SEC 공시 | `data.sec.gov/submissions/CIK{10자리}.json`, UA 헤더 필수, 종목 간 0.15s 간격(초당 10회 한도). SEC엔 산문 제목이 없어 **제목=primaryDocDescription(영문, 없으면 폼 코드)** — 미번역. CIK·SIC는 화이트리스트 수기 관리 대신 `sync_overseas_master`가 실시간 해석 | F-3.2.1, F-4.6 실측 |
| SIC 실측치 | KO=**2080**, SBUX=**5810** (통념 2086/5812와 다름 — SEC submissions 기준으로 매핑표 관리) | F-4.7.1 |
| Federal Register | `documents.json` + `conditions[type][]=RULE·PRORULE` + 기관 슬러그 필터 + `fields[]` 지정, 페이지네이션은 응답의 `next_page_url` 추적. 기관 슬러그는 `/api/v1/agencies` 유효 목록 대조 후 무효분 폐기·로그 | F-4.7 실측 |
| 해외 규제 2차 관련성 | 요약 스키마에 `relevant`(bool) 추가 — false면 **업종 링크 삭제 + 생성물 미생성**(카드 없음) | F-4.7.2 |
| 생성 단위 보강 | 금리 탭(bok/fed) 요약은 종목·업종이 아닌 **전역 1건** — `generated_content.scope="global"` 사용 | F-5.2.1 보강 |

## 7. v3에서 사라졌지만 유지하는 것

- **인기 종목 목록**(구 F-2.3): PRD 종목 추가 창에 인기 칩이 있으므로 유지 — 국내는 시총 상위 8, 해외는 화이트리스트 앞 8개.
- **`GET /markets` 인증**: v3 부록 2는 "세션"인데, 세션 발급 전 첫 호출일 수 있으므로 **불필요(세션 있으면 활용)** 로 완화.

## 8. 공시 노출 정책·정형 API (PM 수정 반영 — 2026-08-14, 이슈 #18)

| 항목 | 확정 |
| :---- | :---- |
| 노출 유형 (F-6.1 수정) | 공시 탭은 **display=true 유형만 노출**: 초보자 유용 3종(단일판매ㆍ공급계약, 영업(잠정)실적, 자기주식취득결과보고서) + 정형 API 6종(자기주식 취득·처분, 유·무상증자, 감자, 소송) + **정기보고서 3종(사업·반기·분기 — PM 옵션 1 확정, 빈 탭 방지)**. 목록은 `data/disclosure_form_types.json`의 display 플래그로 데이터 관리 |
| 수집 범위 | **수집은 전체 유지, 노출만 필터** — 정책 변경 시 재수집 없이 데이터 파일만 수정 |
| 정형 API (F-4.1 확장) | DART 주요사항보고서 상세 6종(`tsstkAqDecsn`·`tsstkDpDecsn`·`piicDecsn`·`fricDecsn`·`crDecsn`·`lwstLg`)을 **접수번호(rcept_no)로 조인**. 매핑표(영문 키→한글 라벨→슬롯)는 `data/dart_detail_map.json` — PM 검수 산출물 |
| 저장 (부록 1 보정) | `source_item.content` = 라벨: 값 텍스트(요약 입력), **`detail_json.slots`** = 카드 배지(3~4개). 카드 스키마에 `details` 필드 추가 |
| F-5.1 입력 확대 | 공시 요약 입력 = 제목 + 유형 해설 + **정형 필드 텍스트**. 금액·기간을 사실 기반으로 요약 — F-5.1.3 숫자 검증과 정합(입력에 있는 숫자만 통과) |
| F-6 실데이터 전환 | `/cards` 목 응답 → DB 조회(source_item + generated_content + rate_link_sentence + saved_card) 전환 완료. 빈 목록 사유는 collect_status 기반 no_data/fetch_failed |

## 9. v3 갱신(2026-08-16, PM) 대응 — 구현과의 차이 정리

PM이 백엔드 실측 문서를 명세에 흡수해 v3를 갱신했다(부록 6). 대부분 구현과 일치하며, 어긋나는 항목의 처리:

| 항목 | 새 명세 | 처리 |
| :---- | :---- | :---- |
| F-3.5 등록 상한 | **10개**(구분별) + 초과 시 남은 자리 수 반환 | ✅ 반영 — `STOCK_LIMIT_PER_MARKET=10`, 400 details에 `limit`·`remaining` |
| F-3.1.0 국내 적재 범위 | KONEX 제외(기존과 동일), **스팩 제외 권고** | ✅ 반영 — 마스터 동기화 시 종목명 "스팩" 제외 |
| 부록 6 "공시 조회 기간 3개월 → 12개월" | 12개월 | ✅ **PM 확정(2026-08-16): 90일·종목당 20건 현행 유지** — 부록 6 표기는 오기 |
| F-3.3.1 화이트리스트 밖 검색 | 안내 없이 빈 배열, 별칭 목록 미관리 | ✅ **PM 확정: `unsupported_overseas` 안내 유지**. PM 희망(전체 해외 종목 검색·등록)은 시가총액·업종 무료 소스 부재 + 종목당 수집·생성·검수 비용으로 시연 범위 밖 — 화이트리스트 확장은 `data/overseas_whitelist.json` 추가로 언제든 가능 |
| F-4.2.2 규제 1차 필터 = 키워드 | 부처는 출처 표기용 | ✅ **PM 확정: 현행(부처 1차→키워드 2차 + 미매핑 로그) 유지** |
| F-4.2.1 "종목명도 키워드에" | 업종 키워드 + 종목명 | 후순위 — 발표문에 종목명 등장이 드묾 |
| 부록 1 `industry_keyword` 테이블 | 별도 테이블 | 구조만 다름(`industry_agency.keywords` JSON) — 데이터 관리 취지 동일, 변경 없음 |
| F-5.6 캡션 "왜 긍정일까요" | 프론트가 label로 조립 | 백엔드 변경 없음 |
| F-3.8 기본 종목 | 시총 상위 4(자동, 날마다 변동 가능) — **유지** | 프론트가 요청한 고정 4종(현대차·NAVER)은 명세와 다름 → PM 결정 필요 |
